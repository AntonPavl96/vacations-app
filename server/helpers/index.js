const { allLoggedUsers } = require("./allLoggedUser");
const { onlyUsers } = require("./onlyusers");
const { onlyAdmins } = require("./onlyAdmins");

module.exports = { allLoggedUsers, onlyUsers, onlyAdmins };
